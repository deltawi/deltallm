apiVersion: v2
name: deltallm
description: DeltaLLM - OpenAI-compatible LLM gateway
type: application
kubeVersion: ">=1.24.0-0"
version: 0.2.0
appVersion: "0.1.0"
dependencies:
  - name: postgresql
    version: "16.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
  - name: redis
    version: "20.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled
