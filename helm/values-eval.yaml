replicaCount: 1

autoscaling:
  enabled: false

podDisruptionBudget:
  enabled: false

migrationJob:
  enabled: true

postgresql:
  enabled: true
  auth:
    username: deltallm
    password: deltallm
    database: deltallm

redis:
  enabled: true
  auth:
    enabled: false
